/*Faca um programa em C que calcule a aerea de um triangulo. Considere base e altura numeros inteiros maior que 0
  */

  #include <stdio.h>

  int main () {
  unsigned int base = 5;
  unsigned int altura = 8;

   float area = base * altura / 2;

printf("a area é %f\n", area);
return 0;
}